# Claude Compatibility

Use `AGENTS.md` in the repository root as the source of truth for repository-wide instructions.

When a task matches a local skill, read the relevant files under `.agents/skills/`.
