# Claude Usage

If you want to use this repository with Claude Code, use the repository root `AGENTS.md` as the default instruction file.

If your Claude setup supports local skills, copy `.agents/skills` to `.claude/skills` in this repository.
